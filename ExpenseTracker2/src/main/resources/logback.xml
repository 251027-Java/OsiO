<configuration>
    <!-- Console appender -->
    <appender name="STDOUT" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{HH:mm:ss.SSS} %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- Set default level to INFO; override with environment or system property if needed -->
    <root level="INFO">
        <appender-ref ref="STDOUT" />
    </root>

    <!-- Increase MongoDB driver logging to WARN by default to reduce noise -->
    <logger name="org.mongodb.driver" level="WARN" />
</configuration>
